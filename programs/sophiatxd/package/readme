To create debian package, run command:

debuild --eDEB_BUILD_OPTIONS="parallel=4" \
        --set-envvar CMAKE_BUILD_TYPE_ENV=Release \
        --set-envvar BUILD_SOPHIATX_TESTNET_ENV=false \
        --set-envvar SOPHIATX_EGENESIS_JSON_ENV=/path/to/genesis \
        --set-envvar OPENSSL_ROOT_DIR_ENV=/path/to/openssl/root \
        --set-envvar BOOST_ROOT_DIR_ENV=/path/to/boost/root \
        -uc -us